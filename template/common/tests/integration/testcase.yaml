tests:
  __MODULE_NAME__.integration:
    tags:
      - drivers
      - __SUBSYS__
    platform_allow:
      - qemu_cortex_m3
    # TODO: Hardware drivers also need boards/qemu_cortex_m3.overlay (emulator bus).
