tests:
  __MODULE_NAME__.integration:
    tags:
      - drivers
      - __SUBSYS__
    platform_allow:
      - mps2/an521
    # TODO: Hardware drivers also need boards/mps2_an521.overlay (emulator bus).
